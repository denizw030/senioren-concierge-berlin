name: Clean public URLs once

on:
  push:
    branches:
      - fix/clean-public-urls-20260919
    paths:
      - 'scripts/clean-url-postprocess.py'
      - 'assets/clean-url.js'
      - 'assets/language-switcher.js'
      - 'assets/app-language-switcher.js'
      - 'assets/auth-i18n.js'
      - 'robots.txt'
      - 'sitemap.xml'
      - '.github/workflows/clean-public-urls-once.yml'

permissions:
  contents: write

jobs:
  normalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Normalize public URLs
        run: python scripts/clean-url-postprocess.py
      - name: Validate SEO and link flow
        run: node --test tests/seo-indexing.test.mjs tests/i18n-link-flow.test.mjs
      - name: Validate diff
        run: git diff --check
      - name: Commit generated clean URLs
        run: |
          if git diff --quiet; then
            echo "No generated URL changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "[clean-url-bot] normalize public URLs"
          git push origin HEAD:fix/clean-public-urls-20260919
